
import {notes} from "notes.js";


function hello() {
    return 'Hello World';
}
function getNotes() {
    return  notes;
}

// process notes (in start, end, frequency, note name format)
// based on given tempo (in musescore format, quarter notes / second)
function processNotes(notes, tempo) {
    for (const note of notes) {
        
    }
}
